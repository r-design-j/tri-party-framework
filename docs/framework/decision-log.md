# Framework Decision Log

| ID | Date | Decision | Reason | Alternatives | Verification |
| --- | --- | --- | --- | --- | --- |
| D-2026-06-01-001 | 2026-06-01 | Define tri-party framework as exactly Codex + Claude + Gemini. | Prevent Codex sub-agents from being mistaken for external model parties. | Leave "three model roles" abstract. | `AGENTS.md` and `tri-party-protocol.md` name the three parties explicitly. |
| D-2026-06-01-002 | 2026-06-01 | Treat local Claude/Gemini CLI outputs as valid party sources. | The user's environment exposes `claude` and `gemini` through shell CLI rather than MCP/model tools. | Require MCP/connectors only. | `scripts/triparty-preflight.sh` checks CLI availability and connectivity. |
| D-2026-06-01-003 | 2026-06-01 | Add executable preflight and review scripts before adding more protocol prose. | Recent failures were execution failures, not just documentation gaps. | Continue manual source checks. | `scripts/triparty-preflight.sh` and `scripts/triparty-review.sh` create archived source-status files. |
| D-2026-06-01-004 | 2026-06-01 | Use capability-based dispatch: Codex for code/repo work, Claude for complex reasoning and long-chain autonomy, Gemini for multimodal and Google-context work; keep concrete model versions in `model-binding.yaml`. | The three-party framework should route work by model strengths instead of treating all parties as interchangeable voters, while avoiding hard-coded model versions across protocol prose. | Equal voting for every task or hard-code versions in every document. | `AGENTS.md`, `README.md`, and `tri-party-protocol.md` include capability roles; `model-binding.yaml` stores current model bindings. |
| D-2026-06-01-005 | 2026-06-01 | Add a merge gate script before final synthesis. | Source-status checks must be executable, not only prose. | Let Codex manually decide whether a run is complete. | `scripts/triparty-merge.sh` creates `merge-status.md`, `merge-input.md`, or `partial-report.md` based on run status. |
| D-2026-06-01-006 | 2026-06-01 | For headless Gemini CLI calls, default to `gemini-3.1-pro-preview` and `--allowed-mcp-server-names __none__`. | The configured `pencil` MCP server was observed as disconnected and default Gemini calls hung or timed out, while disabling MCP and specifying the model restored stable output. | Keep default Gemini CLI behavior. | `scripts/triparty-preflight.sh` and `scripts/triparty-review.sh` both complete with Gemini `Available` / `Completed` in run `review-20260601-102132`. |
| D-2026-06-01-007 | 2026-06-01 | Require mutual cross-audit before any true tri-party synthesis. | Division of labor is not enough; the three parties must supervise and challenge each other's results before Codex merges the conclusion. | Let Codex synthesize immediately after independent Claude/Gemini reviews. | `scripts/triparty-cross-audit.sh` archives Claude/Gemini cross-audits and `scripts/triparty-merge.sh` refuses runs without completed cross-audit artifacts. |
| D-2026-06-01-008 | 2026-06-01 | Package the framework as an agent-agnostic portable core with thin adapters, not as a Codex-only Skill. | Not all users have Codex; the protocol must survive across Claude, Gemini, CLI, MCP, and UI environments. | Make Codex Skill the primary product shape. | `docs/framework/productization-strategy.md` defines the portable core and optional adapters. |
| D-2026-06-01-009 | 2026-06-01 | Add `scripts/triparty.sh` as the unified product entrypoint and generate run-level `state.json`. | A productized core needs one user-facing command and one machine-readable state surface, not only separate stage scripts. | Keep only stage scripts and require users to inspect Markdown files manually. | `scripts/triparty.sh status <run-dir>` writes `state.json`; regression checks ready-state output. |
| D-2026-06-01-010 | 2026-06-01 | Start product externalization with a local HTTP adapter and explicit adapter contract. | A dependency-free local adapter lets UI, CI, MCP wrappers, and other agent runtimes consume the portable core without forking protocol logic. | Build a Codex-only Skill first or require every ecosystem to shell out manually. | `adapters/http/triparty_http_adapter.py`, `docs/framework/adapter-contract.md`, and `scripts/triparty-adapter-smoke.sh` define and test the first external adapter. |
| D-2026-06-01-011 | 2026-06-01 | Add offline injection, resume, run management, structured errors, and MCP adapter before UI. | UI should consume stable recovery, diagnostics, run history, and agent-tool integration rather than inventing those behaviors. | Build UI directly on raw files and manual shell stages. | `scripts/triparty.sh inject/resume/runs/stats/archive`, `state.json` errors/provenance, and `adapters/mcp/triparty_mcp_adapter.py` are covered by regression and smoke tests. |
| D-2026-06-01-012 | 2026-06-01 | Record detailed provenance for user-supplied artifacts and reject injected artifact hash drift at merge. | Claude/Gemini cross-audit flagged provenance and invalid-inject paths as UI-before blockers. | Keep only string-level `user_supplied` provenance. | `state.json` now records origin, injection time, source path/hash, and artifact hash; regression covers invalid inject inputs and tamper rejection. |
| D-2026-06-01-013 | 2026-06-01 | Require artifact metadata and completion markers before merge eligibility. | Final cross-audit flagged that probe success and non-empty artifacts were insufficient proof of completed independent reviews. | Treat any non-empty hashed file as a valid model artifact. | Review, cross-audit, and injected artifacts now receive metadata headers and completion markers; merge rejects missing metadata, party/stage mismatch, marker loss, hash drift, and label contamination. |
| D-2026-06-01-014 | 2026-06-01 | Publish core state/status files atomically and record model-binding hash in preflight. | Final cross-audit flagged partial writes and model-binding drift as UI-before reliability risks. | Let adapters read files while shell redirection is still writing. | `state.json`, status env/status markdown, merge status, and preflight files now use temp-file-and-rename writes where they are core status surfaces; preflight records `MODEL_BINDING_SHA256`. |
| D-2026-06-01-015 | 2026-06-01 | Add a trigger contract with strong and weak activation phrases. | A new-session example showed "三方框架" being interpreted as a design/registry/runtime audit instead of Codex + Claude + Gemini. | Keep relying on informal natural language. | `AGENTS.md`, protocol docs, README, handbook, anti-patterns, and lint checks now distinguish canonical strong triggers from ambiguous weak triggers. |
| D-2026-06-01-016 | 2026-06-01 | Make tri-party activation inheritable across same-workstream follow-ups. | Codex completed GitHub public-launch packaging as an implementation task even though the active workstream was the tri-party framework; the user correctly flagged that the protocol had not been run first. | Require the user to restate the full trigger on every follow-up, or let Codex decide ad hoc. | `AGENTS.md`, protocol docs, README, standards, and anti-patterns now record inherited follow-up triggers; lint checks the context-drop anti-pattern. |
| D-2026-06-01-017 | 2026-06-01 | Harden Gemini CLI calls with headless policy, retry/backoff, diagnostics, sanitization, and merge-blocking runtime-noise checks. | Real runs showed Gemini 3.1 Pro Preview returning 429 capacity errors, disconnected MCP noise, terminal warnings, and ignored run artifact read failures that polluted otherwise completed artifacts. | Treat any non-empty Gemini output as completed, or rely only on manual retries. | Gemini calls now use plan mode, a policy file, disabled MCP allowlist, longer timeout/retry/backoff, sanitized output, policy-hash recording, sanitizer-version diagnostics in `state.json`, release capacity thresholds, and merge runtime-noise checks. |
| D-2026-06-01-018 | 2026-06-01 | Add a release gate for public push/release claims. | The inherited follow-up rule needed a machine-callable gate so public release work cannot rely only on prompt-level self-discipline. | Keep release readiness as a checklist-only practice. | `scripts/triparty.sh run` validates release-ready state after merge, while `triparty-release-gate.sh`, `triparty.sh release-gate`, `triparty-validate-state.py`, and optional pre-push hook installation verify merge/state/schema/hash/provenance readiness before public claims. |
| D-2026-06-01-019 | 2026-06-01 | Add a global new-session bootstrap installer. | New sessions outside the repository could not discover the installed framework and improvised new Markdown files instead of using the existing protocol and scripts. | Keep relying on repository-local `AGENTS.md` only. | `scripts/install-triparty-global-bootstrap.sh` writes global Codex and Claude Code bootstrap instructions, `~/.triparty-framework/config`, and a `triparty` CLI wrapper; repository `CLAUDE.md` imports `AGENTS.md`; docs and anti-patterns now forbid ad hoc framework reconstruction. |
| D-2026-06-01-020 | 2026-06-01 | Add Claude Code slash entrypoints `/triparty` and `/tp` as adapter triggers. | Users need a short explicit invocation path in new Claude Code sessions; slash triggers reduce weak-trigger ambiguity while keeping the core portable. | Depend only on natural language triggers, or make a Codex-only skill. | `.claude/skills/triparty/SKILL.md` creates `/triparty`, `.claude/commands/` provides fallback command files and alias, and the global installer copies them into `~/.claude`. |
| D-2026-06-01-021 | 2026-06-01 | Refine source-label contamination scanning around adapter direct-call language. | Reviews must be able to say an adapter should not directly call `claude` or `gemini` without being mistaken for a claim that those parties were unavailable. | Keep broad `未直接调用 Claude/Gemini` matching. | `scripts/triparty-regression.sh` now accepts descriptive direct-model-call risk text and still rejects explicit false source-status claims such as `Gemini = 未调用`. |
| D-2026-06-01-022 | 2026-06-01 | Make release-gate latest-run selection ignore incomplete preflight-only review directories. | A pre-push gate selected a newer `review-*` directory without `source-status.md` and blocked a valid ready run. | Require users to always pass a run-dir, or keep selecting every newest `review-*` directory. | `scripts/triparty-release-gate.sh` filters default candidates by `source-status.md`, and regression verifies a newer incomplete directory is skipped. |
| D-2026-06-01-023 | 2026-06-01 | Resolve a writable runs directory and record the actual location in state. | Release evidence can be created from new sessions or restricted working directories where `docs/framework/runs` is unavailable. | Assume the repo-local runs path is always writable. | `scripts/triparty-runs-dir.sh`, `scripts/triparty.sh`, preflight/review/release-gate scripts, and the HTTP adapter use the resolver; regression verifies temp fallback creation. |
| D-2026-06-01-024 | 2026-06-01 | Add a Gemini auth doctor before long headless Gemini calls. | Browser-auth waits can consume the full review timeout and produce ambiguous partial runs. | Let the normal Gemini content probe discover auth failures. | `scripts/triparty-gemini-auth-doctor.sh` classifies authenticated, interactive-auth-required, binary-missing, and timeout states; preflight records the result and release validation requires authenticated status. |
| D-2026-06-01-025 | 2026-06-01 | Add manifest-verified continuity checkpoint and bootstrap commands. | Clean release work needs file-backed handoff without treating chat history as source of truth. | Keep continuity as manual summaries. | `scripts/triparty.sh continuity checkpoint/bootstrap` writes current, handoff, bootstrap, manifest, and redaction rules; regression verifies hash acceptance and tamper rejection. |
| D-2026-06-05-026 | 2026-06-05 | Promote AgentParty to the protocol layer and treat `triparty` as the first product pack. | The website and product goal now need a generic multi-agent framework that can host more than Codex + Claude + Gemini. | Keep treating all protocol and adapter docs as triparty-only. | `docs/framework/agentparty-protocol.md`, `docs/framework/agentparty-packs.json`, and website product-pack sections define the protocol/pack split. |
| D-2026-06-05-027 | 2026-06-05 | Add a Claude Code + Feishu Claw product-pack scaffold as a 2-agent AgentParty pack. | The user's most common workflow uses Claude Code with Feishu Claw, but it must not be misreported as true tri-party. | Force the workflow through the `triparty` completion gate or leave it as informal chat guidance. | `docs/framework/product-packs/claude-code-feishu-claw.md` defines transcript evidence, pack-ready/partial labels, and a hard ban on `true_triparty_ready`. |
| D-2026-06-05-028 | 2026-06-05 | Product-pack registry is the source for pack discovery, OS support, and forbidden claims. | Cross-agent skill portability needs a durable contract that UI, adapters, the current CLI scaffold, and future native surfaces can share. | Let every page or adapter hand-code product-pack assumptions. | `docs/framework/agentparty-pack.schema.json`, `docs/framework/agentparty-packs.json`, and `scripts/agentparty-pack-lint.py` validate the registry. |
| D-2026-06-05-029 | 2026-06-05 | Add evidence import and pack-state validation for the Claude Code + Feishu Claw pack. | Prompt generation alone does not solve cross-agent skill degradation; the product pack needs a repeatable way to bring Claw transcript, Feishu links, operation summaries, and Claude review evidence back into a state file. | Leave Claw as a prompt-only scaffold or claim pack readiness manually in chat. | `scripts/agentparty.py evidence/validate-run` imports evidence, writes `agentparty.pack-state.v1`, keeps `true_triparty_ready=false`, and regression covers ready/partial/blocked states. |
| D-2026-06-05-030 | 2026-06-05 | Add OS-specific install plans and managed global-bootstrap cleanup. | A productized framework must tell users how to install and how to safely undo global discovery files without overclaiming native Windows execution. | Keep install guidance as prose only, or require users to manually edit global config files. | `scripts/agentparty.py install-plan` exposes supported/blocked paths; `scripts/uninstall-triparty-global-bootstrap.sh` defaults to dry-run and removes only managed artifacts; regression covers install, dry-run, and execute cleanup in a temp home. |
| D-2026-06-05-031 | 2026-06-05 | Add a native PowerShell cleanup scaffold without shipping native PowerShell execution. | Windows users need a readable dry-run cleanup surface, but PowerShell run/evidence workflows remain unverified and must not be overclaimed. | Offer only the bash uninstaller, or claim full native Windows parity. | `scripts/uninstall-triparty-global-bootstrap.ps1` mirrors dry-run/execute and modified-file skip semantics; `agentparty install-plan --target-os windows_powershell`, lint, regression, README, and website expose it as cleanup-only. |
| D-2026-06-05-032 | 2026-06-05 | Add `agentparty install` as the user-facing managed install entrypoint. | Product packs need a simple AgentParty-level installation command instead of asking users or agents to remember the lower-level bootstrap script. | Keep only `install-plan` plus `scripts/install-triparty-global-bootstrap.sh`. | `scripts/agentparty.py install` defaults to dry-run, requires `--execute`, calls the managed bootstrap only when the requested target matches the detected macOS/Linux/WSL2 host, blocks native PowerShell execute and target spoofing, and regression covers dry-run, execute, Claw pack boundaries, and Windows blocking. |
| D-2026-06-05-033 | 2026-06-05 | Add a standard Claw evidence bundle template and bundle import path. | The Claude Code + Feishu Claw pack needs a repeatable handoff artifact so users are not forced to assemble transcript, link, operation summary, and review flags manually every time. | Keep only long `agentparty evidence` parameter lists, or wait for an automated Feishu connector before standardizing evidence shape. | `scripts/agentparty.py evidence-template` writes `agentparty-claw-evidence.json` plus fill-in files; `agentparty evidence --bundle` imports them, rejects unchanged TODO template files, rejects tiny evidence files, invalid links, artifact path traversal, and run-dir mismatch, keeps `true_triparty_ready=false`, and regression covers bundle and direct-flag imports. |
